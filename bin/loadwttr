#!/bin/bash

FILE="$HOME/.tmp/weather"
CHECK=$(date | awk '{ print $1 }')
DATE=$(awk 'NR==40' $FILE)

File() {
    rm -f $FILE
    curl -Ls wttr.in/tespe > $FILE
    date | awk '{ print $1 }' >> $FILE
}

[[ $CHECK -ne $DATE ]] && File || exit
